# Doc on devcontainers
- run `npm install -g @devcontainer/cli`
- To start `devcontainer up --workspace-folder <directory-path>`
- To run a command `devcontainer exec --workspace-folder . <command-to-run>`

